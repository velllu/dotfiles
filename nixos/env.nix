{
  config = {
    environment.sessionVariables = {
      GIT_ASKPASS = "";
      EDITOR = "hx";
      EZA_ICON_SPACING = "2";
      PF_ASCII = "whatever"; # So the orrible nixos logo does not
                             # show up with pfetch
    };
  };
}

